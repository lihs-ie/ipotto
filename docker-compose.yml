services:
  ipo-api:
    build:
      context: ./services
      dockerfile: ../docker/ipo-api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - FIREBASE_PROJECT_ID=ipotto-local
      - FIRESTORE_EMULATOR_HOST=firebase-emulator:8088
      - FIREBASE_AUTH_EMULATOR_HOST=firebase-emulator:9099
      - PUBSUB_EMULATOR_HOST=pubsub-emulator:8085
      - IPO_BROWSER_BASE_URL=http://ipo-browser:8081
      - IPO_NOTIFICATION_FROM_ADDRESS=no-reply@example.com
      - SENDGRID_API_KEY=test-sendgrid-key
      - SENDGRID_ENDPOINT=http://notification-mock:80/sendgrid/mail/send
      - LINE_NOTIFY_ENDPOINT=http://notification-mock:80/line/api/notify
      - ALLOWED_EMAIL=test@example.com
      - RUST_LOG=info
    depends_on:
      firebase-emulator:
        condition: service_healthy
      pubsub-emulator:
        condition: service_started
      notification-mock:
        condition: service_started

  ipo-browser:
    build:
      context: ./services
      dockerfile: ../docker/ipo-browser/Dockerfile
    ports:
      - "8081:8081"
    shm_size: "1gb"
    environment:
      - PORT=8081
      - FIRESTORE_EMULATOR_HOST=firebase-emulator:8088
      - PUBSUB_EMULATOR_HOST=pubsub-emulator:8085
      - MOCK_SERVER_URL=http://html-mock-server:80
      - IPO_API_BASE_URL=http://ipo-api:8080
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1g
    depends_on:
      firebase-emulator:
        condition: service_healthy
      pubsub-emulator:
        condition: service_started
      html-mock-server:
        condition: service_started

  ipo-info-fetcher:
    build:
      context: ./services
      dockerfile: ../docker/ipo-info-fetcher/Dockerfile
    ports:
      - "8082:8082"
    environment:
      - PORT=8082
      - FIRESTORE_EMULATOR_HOST=firebase-emulator:8088
      - PUBSUB_EMULATOR_HOST=pubsub-emulator:8085
      - IPO_BROWSER_BASE_URL=http://ipo-browser:8081
      - IPO_EXTERNAL_SCRAPER_BASE_URL=http://html-mock-server/ipo-stocks.json
      - RUST_LOG=info
    depends_on:
      firebase-emulator:
        condition: service_healthy
      pubsub-emulator:
        condition: service_started

  ipo-result-checker:
    build:
      context: ./services
      dockerfile: ../docker/ipo-result-checker/Dockerfile
    ports:
      - "8083:8083"
    environment:
      - PORT=8083
      - FIRESTORE_EMULATOR_HOST=firebase-emulator:8088
      - PUBSUB_EMULATOR_HOST=pubsub-emulator:8085
      - IPO_BROWSER_BASE_URL=http://ipo-browser:8081
      - RUST_LOG=info
    depends_on:
      firebase-emulator:
        condition: service_healthy
      pubsub-emulator:
        condition: service_started
      ipo-browser:
        condition: service_started

  ipo-applier:
    build:
      context: ./services
      dockerfile: ../docker/ipo-applier/Dockerfile
    ports:
      - "8084:8084"
    environment:
      - PORT=8084
      - FIRESTORE_EMULATOR_HOST=firebase-emulator:8088
      - PUBSUB_EMULATOR_HOST=pubsub-emulator:8085
      - IPO_BROWSER_BASE_URL=http://ipo-browser:8081
      - RUST_LOG=info
    depends_on:
      firebase-emulator:
        condition: service_healthy
      pubsub-emulator:
        condition: service_started
      ipo-browser:
        condition: service_started

  ipo-frontend:
    build:
      context: ./services
      dockerfile: ../docker/ipo-frontend/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://ipo-api:8080
      - NEXT_PUBLIC_FIREBASE_AUTH_EMULATOR_HOST=firebase-emulator:9099
      - NEXT_PUBLIC_FIRESTORE_EMULATOR_HOST=firebase-emulator:8088
    depends_on:
      - ipo-api
      - firebase-emulator

  firebase-emulator:
    build:
      context: ./docker/firebase-emulator
    ports:
      - "4000:4000"
      - "9099:9099"
      - "8088:8088"
      - "9199:9199"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 15s

  pubsub-emulator:
    image: gcr.io/google.com/cloudsdktool/google-cloud-cli:emulators
    command: >
      gcloud beta emulators pubsub start
      --host-port=0.0.0.0:8085
      --project=ipotto-local
    ports:
      - "8085:8085"

  html-mock-server:
    image: nginx:alpine
    ports:
      - "8090:80"
    volumes:
      - ./services/ipo-browser/tests/fixtures/html:/usr/share/nginx/html:ro

  notification-mock:
    image: nginx:alpine
    ports:
      - "9190:80"
    volumes:
      - ./docker/notification-mock/nginx.conf:/etc/nginx/conf.d/default.conf:ro
